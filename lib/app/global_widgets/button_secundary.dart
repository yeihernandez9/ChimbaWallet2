import 'package:flutter/material.dart';

class ButtonSecundary extends StatelessWidget {
  const ButtonSecundary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
